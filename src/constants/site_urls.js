
const home = "/";
// Auth
const signin = "/signin";
const signup = "/signup";

const accountVerify = "/account/verify";

// School
const school = "/school";
const newSchool = `${school}/new`;
const overview = `${school}/overview`;
const users = `${school}/users`;
const settings = `${school}/settings`;


export {
    home,
    signin,
    signup,
    accountVerify,
    school,
    newSchool,
    overview,
    users,
    settings
}